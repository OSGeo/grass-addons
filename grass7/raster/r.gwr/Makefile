MODULE_TOPDIR = ../..

PGM = r.gwr

LIBES = $(RASTERLIB) $(SEGMENTLIB) $(GISLIB) $(GMATHLIB) $(MATHLIB)
DEPENDENCIES = $(RASTERDEP) $(SEGMENTDEP) $(GISDEP)

EXTRA_LIBS = $(OMPLIB)
EXTRA_CFLAGS = $(OMPCFLAGS)

include $(MODULE_TOPDIR)/include/Make/Module.make

default: cmd
