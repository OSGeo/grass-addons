MODULE_TOPDIR = ../..

PGM = r.sun.mp

LIBES = $(GPROJLIB) $(RASTERLIB) $(GISLIB) $(GDALLIBS) $(MATHLIB) $(GMATHLIB) $(PROJLIB) $(OMPLIB)
DEPENDENCIES = $(GPROJDEP) $(RASTERDEP) $(GISDEP) $(GMATHDEP)

EXTRA_INC = $(PROJINC) $(GDALCFLAGS) $(OCLINCPATH)
EXTRA_CFLAGS = $(OMPCFLAGS)
EXTRA_LIBS = $(OCLLIB) $(GISLIB) -lgomp $(MATHLIB)

#needed?  LIBES += $(OCLLIBPATH)  or  EXTRA_LDFLAGS = $(OCLLIBPATH)

include $(MODULE_TOPDIR)/include/Make/Module.make

ifneq ($(USE_GDAL),)
default: cmd
endif
